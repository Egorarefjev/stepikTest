<section class="quiz">
  <div class="quiz__title">{{this.name}}</div>

  <div class="quiz__form" >
    {{#each this.questions as |question index|}}
      <div class="quiz__question">
        <p class="quiz__question-title">{{question.text}}</p>

        <div class="quiz__options">
          {{#each question.choices as |choice|}}
            <Ui::Checkbox
              @checked={{true}}
              @onChange={{fn this.onToggleAnswer question.id choice}}
              @label={{choice}}
              class="quiz__option"
            />
          {{/each}}
        </div>
      </div>
    {{/each}}

    <Ui::Button @type="primary" {{on 'click' this.submitAnswer}}>
      Отправить
    </Ui::Button>
  </div>
</section>