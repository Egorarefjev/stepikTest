<div class="wrapper">
  <div>Выбери квиз</div>

  <div local-class="quiz-list">
    {{#each this.quizzes as |quiz|}}
      <QuizCard::Item
        @name={{quiz.name}}
        @id={{quiz.id}}
        @onOpenQuizAction={{this.openQuiz}}
      />
    {{/each}}
  </div>
</div>
